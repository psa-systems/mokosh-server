# Migration 20260506000007: DELETED (intentional)

This version was deliberately removed; there is no `20260506000007_*.sql`
migration. The gap between `20260506000006_create_refresh_tokens.sql` and
`20260506000008_create_user_totp.sql` is expected, not a missing file.

This placeholder exists only to document the gap so the sequence does not
read as an accidental drop. sqlx ignores any file in this directory whose
name does not end in `.sql` (see sqlx-core `migrate::source::resolve_blocking`),
so this Markdown file is never loaded or applied as a migration.
